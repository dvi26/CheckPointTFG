import 'package:flutter/material.dart';

/// Colores usados en el tema de la app.
class AppColors {
  static const primary = Color(0xFF9B5CFF);
  static const bg = Color(0xFF0B0B0E);
  static const surface = Color(0xFF121218);
  static const stroke = Color(0xFF262732);
  static const hint = Color(0xFF9AA0A6);
  static const onPrimary = Colors.white;
  static const onBgIcon = Colors.white70;
  static const onBgTextWeak = Color(0xFFB7BAC4);
}
